# Factor Plus V31 — اصلاح قطعی فاکتورهای ذخیره‌شده

- کلیک روی کل ردیف فاکتور ذخیره‌شده، فاکتور را باز می‌کند.
- کلیک روی دکمه «باز کردن» نیز کار می‌کند.
- رویدادها به‌صورت event delegation روی لیست ثبت شده‌اند تا بعد از جستجو/مرتب‌سازی از بین نروند.
- شناسه فاکتور به‌صورت String مقایسه می‌شود.
- cache service worker به v31 ارتقا یافته و app.js/styles.css با query version بارگذاری می‌شوند تا نسخه قدیمی در کش نماند.
- تمام فایل‌های V30 حفظ شده‌اند؛ READMEها و assets حذف نشده‌اند.
