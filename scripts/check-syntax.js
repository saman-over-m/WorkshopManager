const fs=require('fs');const {execFileSync}=require('child_process');const path=require('path');const root=path.join(__dirname,'..');
for(const rel of ['main.js','preload.js','db.js']){execFileSync(process.execPath,['--check',path.join(root,rel)],{stdio:'inherit'});console.log('OK syntax:',rel);}
const html=fs.readFileSync(path.join(root,'index.html'),'utf8');const opens=(html.match(/<script\b/gi)||[]).length,closes=(html.match(/<\/script>/gi)||[]).length;if(opens!==closes)throw new Error('script tag mismatch');if(!html.includes('FactorPlusEmbedded'))throw new Error('FactorPlus bridge missing');if(!html.includes('window.desktopInfo'))throw new Error('desktop bridge missing');console.log('OK HTML:',opens,'script tags');